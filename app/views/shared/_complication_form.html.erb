<%= form_for(@complication) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
<%= f.text_area :content, placeholder: "Compose new complication..." %>
</div>
<%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>